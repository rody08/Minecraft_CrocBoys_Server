# Local build artifacts

This directory is a convenient staging area for plugin JARs that are ready for a
local test or deployment. JAR files here are intentionally ignored by Git; source
code and release notes remain the durable record.
